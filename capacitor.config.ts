import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.styvr.app',
  appName: 'STYVR',
  webDir: 'out', // <--- C'EST ICI : Remplacez 'public' ou 'www' par 'out'
// (Optionnel, supprimez si erreur)
};

export default config;